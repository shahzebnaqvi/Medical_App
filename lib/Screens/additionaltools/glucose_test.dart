import 'package:flutter/material.dart';

class Glucose_Test extends StatelessWidget {
  const Glucose_Test({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
