import 'package:flutter/material.dart';

class Report_Analysis extends StatelessWidget {
  const Report_Analysis({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
