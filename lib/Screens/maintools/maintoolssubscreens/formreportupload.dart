import 'package:flutter/material.dart';

class FormReportUpload extends StatelessWidget {
  const FormReportUpload({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("s"),
    );
  }
}
